# Backend environment variables example
# Copy to .env and fill real values

PORT=5000
MONGO_URI=
JWT_SECRET=

# Supabase (media storage)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Public bucket for user-owned/published cards (existing behavior)
SUPABASE_STORAGE_BUCKET=cards-images

# Cloudinary (required for image uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional
SITE_URL=http://localhost:5173

# App timezone (business calendar rules). Defaults to Asia/Jerusalem if not set.
APP_TZ=Asia/Jerusalem

# Anonymous media must be private + served via signed URLs
SUPABASE_STORAGE_BUCKET_ANON_PRIVATE=anon-cards-private

# TTL for signed URLs (optional)
# 15 minutes (range: 60-1800 seconds)
SUPABASE_SIGNED_URL_TTL_SECONDS=900
