Canonical: docs/typography-ssot.md

Run:

- cd frontend && npm.cmd run check:typography
- cd frontend && npm.cmd run check:typography:boundary

Strict mode: set TYPO_STRICT=1 to make these checks fail (exit 1).
